{
  "name": "decode-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ iherger/decode-calldata",
    "create-local": "graph create --node http://localhost:8020/ iherger/decode-calldata",
    "remove-local": "graph remove --node http://localhost:8020/ iherger/decode-calldata",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 iherger/decode-calldata"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.64.1",
    "@graphprotocol/graph-ts": "0.32.0"
  }
}
