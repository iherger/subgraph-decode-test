{
  "name": "decode-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ iherger/decode-test",
    "create-local": "graph create --node http://localhost:8020/ iherger/decode-test",
    "remove-local": "graph remove --node http://localhost:8020/ iherger/decode-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 iherger/decode-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
